# frontend

# HMCTS Tasks Frontend

React + TypeScript UI for the HMCTS caseworker task manager.

## Running locally

```bash
npm install
npm run dev
```

The app expects the backend API at `http://localhost:8080/api/tasks`.

## Tests

```bash
npm test
```
